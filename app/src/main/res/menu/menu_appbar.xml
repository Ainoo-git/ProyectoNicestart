<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/mySetting"
        android:title="Settings"
        app:showAsAction="never" /> <!-- No se muestra como acción en la barra, solo en el overflow -->

    <item
        android:id="@+id/item1"
        android:title="Copy"
        android:icon="@drawable/baseline_vpn_key_24"
        app:showAsAction="ifRoom">

        <!-- Texto del item -->
        <!-- Icono que se muestra junto al texto -->
        <!-- Se muestra como acción solo si hay espacio en la barra -->

        <menu>
            <item
                android:id="@+id/item_profile"
                android:title="Profile"
                android:icon="@drawable/baseline_person_24"
                app:showAsAction="always" />

            <item
                android:id="@+id/item3"
                android:title="Bottom app bar"
                app:showAsAction="never" />

            <item
                android:id="@+id/item4"
                android:title="Bottom navigation"
                app:showAsAction="never" />


            <item
                android:id="@+id/item5"
                android:title="Signout"
                app:showAsAction="never"/>   <!-- Solo aparece en el menú desplegable -->

        </menu>

    </item>
</menu>